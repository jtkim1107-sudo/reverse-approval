# 리버스 전자결재 시스템

주식회사 리버스의 사내 전자결재 웹앱입니다.
정적 웹앱(GitHub Pages) + **Supabase**(데이터베이스/로그인) 구조로,
모든 직원이 같은 데이터를 실시간으로 공유합니다.

## 주요 기능

**전자결재**
- 지출결의서 기안 → 조직도상 윗사람에게 순차 결재 (승인/반려)
- 최상위 직급(대표)이 기안하면 전결(자동 승인) 처리
- 결재 대기함(건수 배지), 내 기안함, 전체 문서함(검색/필터)
- **푸시 알림** — 내 결재 차례, 승인/반려 결과를 핸드폰으로 알림

**ERP (판매 관리)**
- 매출 입력·조회 — 채널별(스마트스토어/쿠팡/자사몰 등) 월별 집계
- 매입 입력·조회 — 거래처, 단가, 월별 집계
- 재고 현황 — 제품별 매입−판매 재고, **입고 예정(발주 후 미입고)** 과 **가용 재고**, 최근 매입가 기준 재고 평가액
- 발주서 — 작성 → 결재 → 발주 완료 → 입고 처리(부분 가능) → 매입 자동 생성, 품목별 입고 예정 집계
- **수요예측 · 발주 제안** — 최근 판매 속도(7·28·84일 가중)로 하루 평균 판매·소진 예상일을 계산하고,
  재발주점(하루 평균 × (리드타임+안전재고일))에 닿은 품목의 권장 발주 수량을 제안 → 버튼 한 번으로 거래처별 발주서 생성
- 제품 마스터 — 등록/수정/삭제, 검색, CSV 내보내기

**공통**
- PWA — 핸드폰 홈 화면에 앱처럼 설치 가능
- 반응형(PC 중심), 대시보드 요약, JSON 백업

## 사용자

| 이름 | 직급 | 계정 |
|------|------|------|
| 장팀장 | 팀장 (기안/결재) | jang@example.com |
| 김대표 | 대표이사 (최종 결재) | kim@example.com |

비밀번호는 관리자에게 문의하세요. 어느 기기에서 로그인해도 같은 데이터가 보입니다.

## GitHub Pages로 배포하기

1. GitHub에서 새 저장소를 만듭니다 (예: `reverse-approval`)
2. 이 폴더를 push 합니다
3. 저장소 **Settings → Pages → Source**에서 `main` 브랜치 / `/ (root)` 선택 후 저장
4. 1~2분 뒤 `https://<계정명>.github.io/reverse-approval/` 에서 접속

## 데이터 저장 방식

- 모든 데이터(문서, 제품, 사용자)는 **Supabase 클라우드 데이터베이스**(서울 리전)에 저장됩니다.
- 로그인은 Supabase Auth(이메일/비밀번호)를 사용하며, 로그인한 사용자만 데이터를 읽고 쓸 수 있습니다(RLS).
- `js/app.js` 상단의 publishable key는 공개 가능한 키입니다 (권한은 서버 쪽 RLS로 제어).

## 직원 추가 방법 (관리자)

1. Supabase 대시보드 → Authentication → Users → **Add user**로 계정 생성 (auto confirm 체크)
2. SQL Editor에서 `profiles` 테이블에 이름/부서/직급/결재권한 추가

## 폴더 구조

```
index.html      화면 뼈대
css/style.css   디자인 (반응형 포함)
js/app.js       전체 로직 (라우팅, 결재 흐름, 제품 마스터, Supabase 연동)
```
